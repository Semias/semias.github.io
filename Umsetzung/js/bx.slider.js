$(document).ready(function () {
  $(".slider").bxSlider({
    mode: "fade",
    captions: false,
    speed: 3000,
    slideWidth: 1920,
    auto: true,
    autoDirection: "next",
    autoDelay: 3000,
    pause: 10000,
  });
});
